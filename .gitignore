# Build outputs (regenerable via build.cmd)
/bin/
/obj/
*.obj
*.lst
*.exe
*.pdb
*.ilk
*.map

# Editor / tooling local state
/.claude/
*.swp
*~

# Encrypted/plaintext working data — never commit user data
*.zip

# Local run logs and registry exports — diagnostic artifacts, never commit
*.log
*.reg

tools/__pycache__/
